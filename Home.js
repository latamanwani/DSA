// const cart = ['shoes','tshirt','pen']

// createOrder(cart, function(orderId){
//     processToPayment(orderId)
// })

// const promise = createOrder(cart)

// promise.then(function(orderId){
//     processToPayment(orderId)
// })


// createOrdee(cart,function(orderId){
//     processToPayment(orderId,function(paymentInfo){
//         paySummary(paymentInfo,function(){
//             updateBalance()
//         })
//     })
// })

// createOrder(cart)
// .then((orderId) => processToPay(orderId))
// .then((paymentInfo) => showOrderSummary(paymentInfo))
// .then((paymentInfo) => updateBalance(paymentInfo))